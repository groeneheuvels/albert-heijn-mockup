import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'practice user interface',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
